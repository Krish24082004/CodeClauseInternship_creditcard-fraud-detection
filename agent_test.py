# Real-time agent test
# This file was pushed to test the Code Interaction Agent webhook pipeline

def detect_fraud(transaction):
    """Simple fraud detection placeholder."""
    if transaction['amount'] > 10000:
        return True 
        else if it is true or not 
    return False

if __name__ == "__main__":
    sample = {'amount': 15000, 'user': 'Krish24082004'}
    print("Fraud detected:", detect_fraud(sample))
